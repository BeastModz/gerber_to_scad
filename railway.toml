[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn --workers 1 --threads 8 --bind :$PORT gts_service.wsgi"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
